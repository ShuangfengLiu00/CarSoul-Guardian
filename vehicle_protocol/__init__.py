"""CarSoul OS Vehicle Protocol Layer.

The protocol layer defines the public data contracts between the
simulator, digital twin, agent runtime, and external consumers.

Sub-packages:
  - ``vehicle_schema``  — Pydantic v2 data standard (``carsoul-schema``)
  - ``mcp_adapter``     — MCP capability interfaces (Step 2+)
  - ``simulator``       — Virtual vehicle simulator (Step 2+)
"""
